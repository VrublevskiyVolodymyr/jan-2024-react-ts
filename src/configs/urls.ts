const baseURL = "https://jsonplaceholder.typicode.com"

const users = "/users"
const posts = "/posts"
const comments = "/comments"


const urls = {
    users: users,
    posts:posts,
    comments:comments
}

export {baseURL, urls}